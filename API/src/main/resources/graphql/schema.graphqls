type Query {
    users: [User]
    user(id: ID!): User
    saveUser(username: String!, email: String!, password: String!, nationality: String!, phoneNumber: String!): User
    updateUser(id: ID!, username: String!, email: String!, password: String!, nationality: String!, phoneNumber: String!): User
    deleteUser(id: ID!): Boolean
}

type User {
    id: ID!
    username: String!
    email: String!
    password: String!
    nationality: String!
    phoneNumber: String!
}